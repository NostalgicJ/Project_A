# 화장품 성분 조합 분석 딥러닝 프로젝트 아키텍처

## 🎯 프로젝트 개요
화장품 성분 조합을 분석하여 사용자에게 안전한 조합과 시너지 조합을 추천하는 딥러닝 기반 웹 애플리케이션

## 🏗️ 시스템 아키텍처

### 1. 데이터 레이어 (Data Layer)
```
📁 Data Sources
├── processed_cosmetics_final_2.csv (2,169개 제품)
├── integrated_product_ingredient_normalized_2.csv (92,809개 성분 레코드)
└── coos_master_ingredients_cleaned.csv (25,426개 정제된 성분명)
```

### 2. 데이터 처리 레이어 (Data Processing Layer)
```
📁 Data Pipeline
├── 데이터 정제 및 정규화
├── 성분 벡터화 (Word2Vec/Embedding)
├── 성분 조합 매트릭스 생성
└── 학습 데이터셋 구성
```

### 3. 머신러닝 레이어 (ML Layer)
```
📁 Deep Learning Models
├── 성분 조합 분석 모델 (Transformer 기반)
├── 안전성 분류 모델 (CNN + LSTM)
├── 시너지 효과 예측 모델 (GNN)
└── 추천 시스템 (Collaborative Filtering)
```

### 4. API 레이어 (API Layer)
```
📁 FastAPI Backend
├── /search - 화장품 검색 API
├── /analyze - 성분 조합 분석 API
├── /recommend - 추천 API
└── /health - 헬스체크 API
```

### 5. 프론트엔드 레이어 (Frontend Layer)
```
📁 React Web Application
├── 화장품 검색 인터페이스
├── 조합 분석 결과 시각화
├── 추천 결과 표시
└── 사용자 피드백 수집
```

### 6. 배포 레이어 (Deployment Layer)
```
📁 AWS Infrastructure
├── EC2 인스턴스 (웹 서버)
├── S3 (모델 저장소)
├── RDS (데이터베이스)
└── CloudFront (CDN)
```

## 🔄 데이터 플로우

1. **데이터 수집**: CSV 파일에서 화장품 및 성분 데이터 로드
2. **데이터 전처리**: 성분명 정규화, 벡터화
3. **모델 학습**: 딥러닝 모델 훈련
4. **API 서빙**: FastAPI로 모델 서빙
5. **웹 인터페이스**: React로 사용자 인터페이스 제공

## 🧠 딥러닝 모델 설계

### 1. 성분 임베딩 모델
- **목적**: 화장품 성분을 벡터로 변환
- **기법**: Word2Vec 또는 BERT 기반 임베딩
- **출력**: 각 성분의 128차원 벡터

### 2. 조합 분석 모델
- **목적**: 성분 조합의 안전성 및 시너지 분석
- **기법**: Transformer + CNN
- **입력**: 성분 조합 벡터
- **출력**: 안전성 점수, 시너지 점수

### 3. 추천 모델
- **목적**: 사용자 맞춤 화장품 조합 추천
- **기법**: Graph Neural Network (GNN)
- **입력**: 사용자 선택 화장품들
- **출력**: 추천 화장품 및 조합 분석

## 📊 성분 조합 분석 규칙

### 안전하지 않은 조합 (Avoid Combinations)
1. **비타민C + 레티놀**: 산화 반응으로 효과 상쇄
2. **AHA/BHA + 레티놀**: 과도한 각질 제거
3. **니아신아마이드 + 비타민C**: pH 불일치

### 시너지 조합 (Synergy Combinations)
1. **비타민C + 비타민E**: 항산화 효과 증대
2. **히알루론산 + 세라마이드**: 보습 효과 증대
3. **나이아신아마이드 + 아연**: 모공 관리 효과 증대

## 🛠️ 기술 스택

### Backend
- **Python 3.9+**
- **FastAPI** (웹 프레임워크)
- **PyTorch/TensorFlow** (딥러닝)
- **Pandas/NumPy** (데이터 처리)
- **SQLAlchemy** (ORM)

### Frontend
- **React 18**
- **TypeScript**
- **Material-UI** (UI 컴포넌트)
- **Chart.js** (데이터 시각화)

### Infrastructure
- **AWS EC2** (서버)
- **AWS S3** (파일 저장)
- **Docker** (컨테이너화)
- **Nginx** (리버스 프록시)

## 📈 성능 목표

- **응답 시간**: API 응답 < 2초
- **정확도**: 조합 분석 정확도 > 85%
- **동시 사용자**: 100명 동시 접속 지원
- **가용성**: 99.9% 업타임

## 🔒 보안 고려사항

- **데이터 암호화**: 민감한 사용자 데이터 암호화
- **API 인증**: JWT 토큰 기반 인증
- **입력 검증**: 사용자 입력 데이터 검증
- **HTTPS**: SSL/TLS 암호화 통신

